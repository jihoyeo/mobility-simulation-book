# 교재 요약 — 장별 구성과 핵심 내용

강의 준비용 문서입니다. 각 장이 무엇을 다루고, 학생이 무엇을 만들고, 수업에서 어디를 짚어야 하는지를 적었습니다. 책에는 포함되지 않습니다.

- 본문: `ko/ch00_setup.md` ~ `ko/ch12_metrics.md`, 부록 `ko/appendix_b_ops.md` (부록 A 스마트카드는 목차에서 뺐고 파일만 남아 있습니다)
- 프로젝트: `ko/projects/project_a.md`, `project_b.md` — 스타터 코드는 `projects/`
- 실습: `labs/` — 장마다 노트북 하나. 채점받는 셋의 자가 채점은 `python labs/check.py all`
- 실습 코드: `smartmob/` — 검증 기준은 `tests/` 에 있습니다
- 집필 규칙: `docs/STYLE.md`, 검사기 `tools/kolint.py`
- 12주 배치: [smart-transport-logistics/schedule.md](https://github.com/jihoyeo/smart-transport-logistics/blob/main/schedule.md)

---

## 한눈에 보기

| 장 | 제목 | 여는 질문 | 학생이 만드는 것 | 주차 |
|---|---|---|---|---|
| 0 | 환경 준비 | 이 책의 코드를 어떻게 돌리나 | 첫 시뮬레이션 실행 | 1 |
| 1 | 왜 시뮬레이션인가 | 택시 80대, 평균 대기 4.1분에 가동률 27%는 적정한가 | — (개념) | 1 |
| 2 | 도로망 데이터 | 도로망은 어떻게 생겼나 | 인접 리스트, 통행수단 필터, KD-트리 스냅 | 2 |
| 3 | 최단경로 — 다익스트라와 A\* | 하남시청→미사역 몇 분인가 | **다익스트라 · A\*** | 3 |
| 4 | 시간대별 속도와 실제 라우팅 엔진 | 오전 8시에도 5분 32초인가 | 시간대 비용 실험 | 4 |
| 5 | GTFS와 한국 대중교통 | 버스로 가면 몇 분인가 | GTFS 읽기·필터링 | 4 |
| 6 | 대중교통 경로 탐색 — RAPTOR | 시간표가 있으면 왜 다른가 | **RAPTOR 150줄** | 5 |
| 7 | 환승·요금과 지표 | 환승 몇 번, 요금 얼마 | 통합환승요금, 지표 분포 | 6 |
| 8 | 통행 수요와 수요 생성기 | O-D 집계값을 요청 목록으로 바꾸기 | 수요 생성 3단계 | 7 |
| 9 | 통행시간 예측 모델 | 라우팅 3만 번을 어떻게 감당하나 | LightGBM ETA 모델 | 10 |
| 10 | 배차와 할당 문제 | 비용행렬의 총비용 최소화 | 탐욕 vs SciPy 할당 해법, TSP | 10 |
| 11 | 이산시간 시뮬레이션 루프 | 부품을 어떻게 굴리나 | **시뮬레이션 루프 150줄** | 11 |
| 12 | 결과 읽기 | 차를 세 배 늘렸는데 왜 그대로인가 | 지표·파레토·시각화 | 7·12 |
| A | 스마트카드 데이터 | 실제로 누가 언제 탔나 | (과제·프로젝트용) | — |
| B | 서버 운영 | 서버를 어떻게 띄우나 | (교수자·프로젝트용) | — |
| P-A | 기말 프로젝트 | 무당이는 얼마나 기다리게 하나 | 무당이 GTFS·조사표·시나리오 (팀) | 6~12 |
| P-B | P-실무프로젝트 | 내 동네 버스 노선은 어떻게 고치나 | 대상지 GTFS·후보안·파레토 (팀) | 종강 후 |

**굵게 표시한 셋**만 라이브러리 없이 직접 짭니다. 나머지는 완성 코드를 읽고 인자를 바꿔 실험합니다.

---

## 0장 환경 준비

**여는 상황** — 이 책의 코드는 전부 실행됩니다. 화면의 숫자가 학생 노트북에서도 같아야 합니다.

**절 구성**
`0.1` 설치 · `0.2` 데이터 확인 · `0.3` 엔진 연결 · `0.4` 첫 시뮬레이션 · `0.5` 분 단위 운행 기록

**수업에서 짚을 것**

- **서버가 없어도 책이 멈추지 않습니다.** `SMARTMOB_OFFLINE=1` 이면 `data/fixtures/` 의 녹화본을 씁니다. 집에서 복습할 때 설치할 것이 없다는 점을 첫 시간에 확실히 해 둡니다.
- `1080` = 18:00. 이 책의 모든 시간은 자정부터의 분입니다. 여기서 못 박지 않으면 계속 헷갈립니다.
- 0.5절의 마지막이 중요합니다. **"차량 80대"가 항상 80대가 아닙니다.** 근무 시간이 끝나면 빠집니다. 결과를 잘못 읽지 않으려면 이런 것을 미리 알아야 합니다.

**나오는 수치** — 하남 도로망 노드 12,566 / 엣지 28,589. 시뮬레이션 결과 서비스율 1.0, 평균 대기 4.08분, 가동률 0.27.

**연습문제** — 0.1(★) 대기 승객이 가장 많았던 시각 찾기

---

## 1장 왜 시뮬레이션인가

**여는 상황** — 0장에서 돌린 하남시 택시 80대의 결과입니다. 평균 대기 4.1분에 가동률 27%, 그래서 80대가 적정한가.

**절 구성**
`1.1` 좋은 모빌리티 시스템이란 · `1.2` 왜 시뮬레이션이 필요한가(+기존 도구 비교) · `1.3` 차량 80대는 적정한가 · `1.4` 시뮬레이터의 구조

**수업에서 짚을 것**

- 1.1절의 세 관점(승객·운영자·도시)이 12장 지표로 그대로 돌아옵니다. 여기서 한 번 심어 둡니다.
- 1.2절의 요지: 실제 도시에서는 같은 기간을 조건만 바꿔 다시 관찰할 수 없다는 것. 저녁을 몇 번 굴려도 시나리오 하나뿐입니다.
- VISSIM/SUMO를 "성능이 나빠서"가 아니라 **"답하도록 만들어진 질문이 달라서"** 안 쓴다고 설명합니다. 그림 `fig-vissim` 이 교차로 하나인 것을 보여 주면 바로 이해합니다.
- 1.3절이 학기 전체의 동력입니다. 승객을 태운 차가 평균 10대인데 빈 차가 31대. **"절반으로 줄이면?"** 이 질문에 11장에서 답합니다.
- 1.4절 그림(`fig-framework`)의 각 상자를 장 번호와 연결한 표가 있습니다. 학기 지도로 씁니다.

**나오는 수치** — 대기시간 중앙값 3.0분, 90분위 8.3분, 최댓값 41.6분. 승객 태운 차 평균 9.9대, 빈 차 31.3대.

**연습문제** — 1.1(★) 오래 기다린 승객의 호출 시각 분포 · 1.2(★★) 세 관점의 지표 설계

---

## 2장 도로망 데이터

**여는 상황** — 0장의 계산이 서 있는 바닥을 직접 열어 봅니다.

**절 구성**
`2.1` 표 두 개 · `2.2` 양 끝 노드는 어디에 · `2.3` 엣지의 절반은 자동차가 못 다닌다 · `2.4` 인접 리스트 · `2.5` 좌표를 노드에 붙이기 · `2.6` 실습

**수업에서 짚을 것**

- **2.2절이 이 장의 핵심입니다.** 엣지 표에 `source`/`target` 컬럼이 없습니다. `edge_id` 를 `rsplit("_", 2)` 로 쪼개야 합니다. 그리고 **짐작한 규칙을 그 자리에서 검증합니다**(`osm_node_seq_json` 과 대조, 2,000개 중 0개 불일치). 데이터를 다루는 태도를 여기서 가르칩니다.
- 2.3절: 하남 엣지 59,873개 중 `footway` 22,064 + `cycleway` 5,100. **거르지 않으면 자동차가 계단으로 다닙니다.** 총 연장 3,994km → 2,082km.
- 2.4절: 비용은 거리가 아니라 **소요시간**입니다. `length / speed`. 3장에서 그대로 씁니다.
- 2.5절: 전수 탐색 수십 ms → KD-트리 1,000건이 순식간. 시뮬레이션에서 승객마다 스냅해야 하므로 이 차이가 누적됩니다.

**연습문제** — 2.1(★) 도로 종류별 연장 · 2.2(★★) 도보 15분 도달 영역 · 2.3(★★★) 실제 형상 geometry 그리기

---

## 3장 최단경로 — 다익스트라와 A\* (직접 구현 1/3)

**여는 상황** — 하남시청→미사역, 직선 3km. 도로로는 몇 분인가.

**절 구성**
`3.1` 문제 적기 · `3.2` 다익스트라의 아이디어 · `3.3` 구현 · `3.4` 맞는지 확인 · `3.5` A\* · `3.6` 실행시간 비교 · `3.7` 전용 엔진 호출 · `3.8` 정리된 코드

**수업에서 짚을 것**

- 3.2절의 한 문장으로 알고리즘 전체가 설명됩니다. "미확정 노드 중 가장 가까운 것은 이미 최종 답이다." **왜 그런지**를 음수 비용이 없다는 조건과 함께 설명합니다.
- 3.2절 끝에 노드 여섯 개 그래프와 힙에서 꺼내는 순서 표가 있습니다. 화요일에는 여기까지만 열고, 학생이 이 표를 보며 `.py` 빈칸을 채운 뒤 수요일에 3.3절 구현을 엽니다. 노트북의 작은 그래프가 같은 것입니다.
- 3.4절: NetworkX 와 50쌍 대조. 실행되는 것과 맞는 것을 구분하는 습관을 학기 내내 반복합니다.
- 무작위 200쌍 중 18쌍은 방향을 따라 도달할 수 없습니다. 시뮬레이터에서 이 경우를 처리하지 않으면 예외와 함께 중단됩니다.
- 3.6절의 무작위 30쌍에서 확정 노드 중앙값은 7,320개에서 3,978.5개로 줄지만 실행시간은 늘어납니다. 탐색량과 실행시간을 따로 측정해야 합니다.

**나오는 수치** — 하남시청→미사역 5분 32초, 경로 노드 83개, 확정 노드 4,565개(전체의 3분의 1).

**연습문제** — 3.1(★) 조기 종료 유무 비교 · 3.2(★★) 휴리스틱에 1.5배 곱하기 · 3.3(★★★) 양방향 다익스트라

---

## 4장 시간대별 속도와 실제 라우팅 엔진

**여는 상황** — 5분 32초는 자유류 속도로 계산한 값입니다. 오전 8시에도 그럴까요.

**절 구성**
`4.1` 속도 컬럼 확인 · `4.2` 시간대별 통행시간 · `4.3` 시간대별 경로 비교 · `4.4` 배차 비용 계산 횟수 · `4.5` 실제 엔진 호출 · `4.6` 하루 곡선

**수업에서 짚을 것**

- 자동차 통행 엣지의 자유류 중앙값은 30.0 km/h이고 시간대별 관측 중앙값은 18.9~22.3 km/h입니다.
- 관측이 없는 엣지가 21.7%입니다. 자유류로 채우면 일부 이면도로의 비용이 작아질 수 있습니다.
- 4.3절에서는 오전 첨두와 심야 경로를 비교합니다. 무작위 50쌍 중 21쌍에서 노드열이 달랐지만, 결측 속도를 자유류 속도로 채운 영향도 함께 들어 있습니다.
- 4.4절: 시뮬레이션 한 번에 질의 3만 회 이상. 파이썬 다익스트라로는 몇 분. 9장과 실제 엔진이 필요한 이유가 여기서 나옵니다.
- 4.5절은 공개 DTUMOS가 OSRM을 사용한다는 범위까지만 설명하고, 서버 내부 알고리즘은 단정하지 않습니다.

**나오는 수치** — 하남시청→미사역: 자유류 5.54분, 심야 7.96분, 오전첨두 8.14분, 오후첨두 8.99분(자유류 대비 62% 증가).

**연습문제** — 4.1(★) 도로 종류별 감속 · 4.2(★★) 결측 채우는 방식 비교 · 4.3(★★★) 시간의존 최단경로

---

## 5장 GTFS와 한국 대중교통 데이터

**여는 상황** — 버스는 아무 때나 출발하지 않습니다. 시간표가 있습니다.

**절 구성**
`5.1` 표 다섯 개 · `5.2` `route_type` 코드 확인 · `5.3` 시각이 24시를 넘는다 · `5.4` 정류장 지도 · `5.5` 잘라 낼 때 노선 전체를 남긴다 · `5.6` 실습

**수업에서 짚을 것**

- 노선(route)과 운행(trip)은 다른 단위입니다. 340번은 노선 하나이고 시간표의 출발편은 각각 별도의 운행입니다.
- 하남시 자료의 `route_type` 은 TAGO 코드입니다. `0` 을 국제 명세대로 트램으로 읽으면 시내·농어촌·마을버스 132개를 잘못 분류합니다.
- 24시를 넘는 시각(`25:30:00`)이 왜 필요한지 설명합니다. 밤 11시 50분 출발, 0시 20분 도착을 `00:20:00` 으로 쓰면 순서가 뒤집힙니다.
- 5.5절: 정류장만 잘라 내면 노선이 시 경계에서 끊깁니다. **정류장 → 운행 → 노선 → 그 노선의 모든 운행** 순으로 되짚어 올립니다.

**나오는 수치** — 하남 GTFS: 정류장 4,203, 노선 169, 운행 8,923, 시각표 656,385행. 24시 넘는 행 22,500개. 시내버스 132노선, 도시철도 15.

**연습문제** — 5.1(★) 첫차·막차 · 5.2(★★) 시간대별 운행 편수 · 5.3(★★★) 노선당 정류장 순서 종류 세기(6장 복선)

---

## 6장 대중교통 경로 탐색 — RAPTOR (직접 구현 2/3)

**여는 상황** — 하남시청에서 오전 8시 출발, 대중교통으로 미사역까지.

**절 구성**
`6.1` 시간표의 그래프 표현 · `6.2` 패턴으로 묶기 · `6.3` 정류장→노선 역색인 · `6.4` 도보 환승 · `6.5` 알고리즘 · `6.6` 경로 복원 · `6.7` 검증

**수업에서 짚을 것**

- 6.1절: **같은 구간의 비용이 도착 시각에 따라 달라집니다.** 8시 3분에 도착하면 12분 기다리고 8시 정각이면 1분입니다. 그래서 그래프 최단경로를 못 씁니다.
- 시각-정류장 쌍을 노드로 두면 65만 개짜리 그래프가 됩니다. RAPTOR 는 대신 **환승 횟수를 라운드로** 삼습니다. 탐색 중에는 우선순위 큐가 없고, 정렬은 준비 단계에서 한 번만 합니다.
- 6.5절 첫머리의 정류장 다섯 개 시간표와 라운드 표가 이 장의 작은 예제입니다. 알고리즘 코드(collect_patterns / scan_pattern / walk_transfers / raptor_core 네 셀)를 열기 전에 이 표로 라운드 개념을 심습니다. 6.2절의 `bisect_left` 세 줄이 실습 `earliest_trip` 의 전부입니다.
- **6.2절이 준비의 핵심입니다.** `route_id` 를 그대로 쓰면 안 됩니다. 정류장 순서가 같은 운행끼리 다시 묶습니다. 운행 8,923개 → 패턴 349개. 노선 하나가 패턴 22개를 갖는 경우도 있습니다(지선·시간대별 단축·회차).
- 6.5절 알고리즘의 전부는 "손에 든 차"입니다. 패턴을 훑으며 타고 있으면 계속 타고, 더 이른 차가 있으면 갈아탑니다.
- **6.7절 검증 방식을 강조합니다.** 실제 GTFS 로는 답을 손으로 확인할 수 없으므로, 6.5절의 작은 시간표를 `tests/test_raptor.py` 가 그대로 씁니다. 실제 피드에서는 불변식만 봅니다(늦게 출발했는데 더 일찍 도착할 수 없다). 새벽 3시 출발은 첫차를 기다려 4,195개에 닿고 미사역 도착이 05:14 입니다. "도보만 남는다"가 아닙니다.

**나오는 수치** — 도보 환승 28,818쌍. 오전 8시 출발 시 4,156/4,203 정류장 도달. 하남시청→미사역 부근 오전 8시 22.9분(차내 6.6 / 도보 11.2 / 대기 5.1).

**연습문제** — 6.1(★) 출발 시각별 통행시간 곡선 · 6.2(★★) 라운드별 도달 정류장 · 6.3(★★★) 등시선 지도

---

## 7장 환승·요금과 대중교통 지표

**여는 상황** — 6장에서 정하지 않고 넘어간 값들을 정합니다.

**절 구성**
`7.1` 환승 몇 번까지 · `7.2` 도보 환승 몇 미터 · `7.3` 요금 · `7.4` 지표 여섯 개 분포 · `7.5` 통행시간 구성 · `7.6` 환승과 요금 · `7.7` 실제 엔진 대조

**수업에서 짚을 것**

- 하남시청 오전 8시에는 환승을 한 번 허용할 때 도달 정류장이 1,679개에서 4,152개로 늘고, 두 번 허용하면 4,156개가 됩니다.
- 도보 환승을 200 m에서 1,000 m로 늘리면 환승 쌍은 7,818개에서 92,574개로 늘고 도달 정류장은 15개 늘어납니다.
- 7.3절의 요금 코드는 높은 기본요금 하나와 10 km 초과 거리 가산을 반영한 교육용 단순화 모형입니다.
- 300개 목적지 표본의 평균 통행시간은 65.5분, 중앙값은 56.2분입니다. 최댓값 경로는 오후 11시 31분 N31번까지 856.2분을 기다립니다.
- 3시간 이하 표본의 시간 합계 비중은 차내 72.5%, 도보 14.3%, 대기 13.2%입니다.

**연습문제** — 7.1(★) 요금 손 계산 · 7.2(★★) 출발지 바꿔 비교 · 7.3(★★★) 30분 환승 제한 넣기

---

## 8장 통행 수요와 수요 생성기

**여는 상황** — 0장에서 입력한 하남시 호출 1,000건을 만드는 절차입니다.

**절 구성**
`8.1` O-D 데이터 · `8.2` 시간대 패턴 · `8.3` 공간 패턴 · `8.4` 시뮬레이터가 받는 형식 · `8.5~8.7` 수요 만들기 3단계 · `8.8` 엔진에 넣기

**수업에서 짚을 것**

- O-D 표는 행정동과 시간대별 집계값입니다. 개인의 정확한 출발 시각과 위치는 따로 정해야 합니다.
- 8.3절: 하남 출발 통행량의 54%는 목적지도 하남이고, 46%는 하남 밖으로 이동합니다. 분석 범위를 정할 때 제외되는 통행을 확인합니다.
- 8.4절: 컬럼 다섯 개 계약. `validate_demand` 가 분/초 혼동과 위경도 뒤바꿈을 잡는 것을 실행해 보입니다.
- 8.5~8.6절은 경계 안 균등 표집과 도로 엣지 위 표집의 위치 분포를 비교합니다.
- 도로 위 표집도 토지 이용과 실제 O-D 관계를 반영하지 않습니다.

**나오는 수치** — 하남 O-D 83,135행. 오전 8시 65,688명 최고, 오후 5~6시 61,573명. 하남 내부 통행 54%.

**연습문제** — 8.1(★) 오전·저녁 상위 O-D 쌍 비교 · 8.2(★★) O-D 쌍 가중 샘플링 · 8.3(★★★) 택시 수요 추정과 민감도

---

## 9장 통행시간 예측 모델(ETA)

**여는 상황** — 배차 비용행렬의 최단경로 계산을 ETA 모델로 근사합니다.

**절 구성**
`9.1` 학습 자료 만들기 · `9.2` 기준선 · `9.3` 선형회귀 · `9.4` 그래디언트 부스팅 · `9.5` 특징별 분할 횟수 · `9.6` 실행시간 비교 · `9.7` 오차 분포

**수업에서 짚을 것**

- 학습 목표값은 3장의 다익스트라로 계산합니다.
- 특징은 모델 사용 시점에 라우팅 없이 계산할 수 있어야 합니다. `network_km`은 입력에서 제외합니다.
- 방위각을 `sin`/`cos` 두 개로 나누는 이유: 359도와 1도가 숫자로는 358만큼 떨어져 있습니다.
- 기준선은 직선거리와 학습 자료의 평균 속도로 계산하며 MAE는 2.52분입니다.
- 거리 하나를 쓴 선형회귀의 MAE는 2.19분이고, 여덟 특징을 쓰면 2.16분입니다. LightGBM은 0.89분입니다.
- `feature_importances_`는 분할 횟수입니다. 인과효과나 특징 제거 시 성능 저하량으로 해석하지 않습니다.
- 실행시간은 같은 환경에서 다시 측정하고, 배차 적용 여부는 차량 순위와 할당 일치율로 평가합니다.

**나오는 수치** — 기준선 MAE 2.52 / 선형(거리만) 2.19 / 선형(8특징) 2.16 / LightGBM 0.89분, R² 0.954. 거리 구간별 LightGBM MAE는 0.85~0.94분입니다.

**연습문제** — 9.1(★) 나무 개수 · 9.2(★★) 제거 실험 · 9.3(★★★) 우회를 잡는 특징 만들기

---

## 10장 배차와 할당 문제

**여는 상황** — 여러 요청의 최근접 차량이 겹칠 때 일대일 배정을 계산합니다.

**절 구성**
`10.1` 비용행렬 · `10.2` 탐욕 배차 · `10.3` 할당 문제 · `10.4` 무작위 표본의 총비용 차이 · `10.5` 구현별 실행시간 · `10.6` 픽업 소요시간 계산 · `10.7` 물류 배정과 경로 · `10.8` 루프 안의 자리

**수업에서 짚을 것**

- 20!은 약 243경이므로 완전탐색은 작은 비용행렬의 검증에만 사용합니다.
- 무작위 200회에서 탐욕 배차의 총비용은 최적해보다 평균 14.4%, 최대 78.8% 높습니다.
- SciPy의 `linear_sum_assignment`는 현재 수정 Jonker–Volgenant 알고리즘을 사용합니다. 실행시간의 교차 지점은 환경마다 다시 측정합니다.
- 직선거리, ETA 모델, 도로망 라우팅의 배정 일치율과 라우팅 기준 총비용을 비교합니다.
- 일대일 배정과 여러 배송지의 방문 순서를 구분하고, 후자는 TSP·VRP로 다룹니다.

**연습문제** — 10.1(★) 처리 순서 바꾸기 · 10.2(★★) top-k 가지치기 · 10.3(★★★) 목적함수 바꾸기(최대 대기 최소화)

---

## 11장 이산시간 시뮬레이션 루프 — 직접 구현 3/3

**여는 상황** — 수요, 소요시간 모형, 배차 방법을 1분 단위 루프에 연결합니다.

**절 구성**
`11.1` 분 단위 처리 순서 · `11.2` 차량과 요청 상태 · `11.3` 루프 · `11.4` 6시간 실행 · `11.5` DTUMOS 녹화본과 비교 · `11.6` 대기시간 구성 · `11.7` 차량 대수별 결과 · `11.8` 배차 방법별 결과

**수업에서 짚을 것**

- 차량의 대기 여부는 `location`, `free_at`, 근무시간으로 계산합니다.
- 녹화본에는 요청 990건, 로컬 입력에는 1,000건이 있어 동일 입력에 대한 구현 검증은 아닙니다.
- 평균 대기는 4.28분(로컬)과 4.08분(녹화본), 운행 차량 시계열 상관계수는 약 0.915입니다.
- 대기 승객이 0명인 분은 각각 99.4%와 98.9%입니다. 분산이 작은 시계열은 상관계수 외의 분포 지표도 함께 봅니다.
- 포기 기준 10분은 배차 전 대기에만 적용되며 총 대기시간에는 픽업 이동시간도 들어갑니다.
- 차량 80대에서는 두 배차 방법의 평균 대기가 4.282분과 4.284분이고, 차량을 줄이면 차이가 커집니다.

**나오는 수치** — 하남 6시간 시뮬레이션 1초 미만. 차량 20/40/60/80대 비교표. 우리 루프 가동률 0.67 은 엔진의 0.27 과 정의가 다릅니다(태우러 가는 시간 포함 여부).

**연습문제** — 11.1(★) 포기 기준 바꾸기 · 11.2(★★) ETA 모델을 루프에 넣기 · 11.3(★★★) 재배치 구현

---

## 12장 결과 읽기 — 지표와 시각화

**여는 상황** — 차를 세 배 늘렸는데 평균 대기가 그대로입니다. 숫자가 거짓말하는 게 아니라 우리가 잘못 읽고 있습니다.

**절 구성**
`12.1` 지표 세 무리 · `12.2` 평균은 왜 위험한가 · `12.3` 분포 · `12.4` 차량 몇 대 · `12.5` 엔진 결과와 같은 표로 · `12.6` 시간대별 · `12.7` 움직이는 것 보기 · `12.8` 결과를 웹으로 내보내기 · `12.9` 보고서에 무엇을 적나

**수업에서 짚을 것**

- **12.2절이 이 장의 전부입니다. 생존 편향.** 차량 20대에서 평균 대기 7.32분, 80대에서 4.28분. 차이가 작아 보이지만 20대에서는 **39%만 탔습니다.** 대기시간은 배차받은 사람만 계산됩니다.
- 포기한 승객에게 30분을 매기면 순위가 뒤집힙니다(20대 21분, 80대 4.28분). **어느 쪽도 틀리지 않습니다. 답하려는 질문이 다를 뿐입니다.** 서비스율과 대기시간을 항상 같이 적습니다.
- 12.4절 파레토 프론트: **데이터가 답을 정해 주지 않습니다.** 곡선은 70대에서 꺾입니다(서비스율 0.999→1.0, 가동률 0.78→0.67). 최종 선택은 사람이 한다는 것을 분명히 합니다.
- 12.5절: 우리 루프와 엔진의 거리가 두 배 차이 납니다. 엔진의 `trip.json` 은 통행의 10%만 저장하기 때문입니다(`viz_ratio=0.1`). **비율(공차 19% vs 18.6%)은 맞습니다.** "남의 결과와 숫자가 다르면 알고리즘보다 정의와 표본을 먼저 확인한다."
- 12.7절: `trip.json` 에 **좌표가 빈 구간이 실제로 섞여 있습니다.** 길이가 같다고 가정하면 시각화가 터집니다. pydeck 출력을 노트북에 저장하지 않는 이유(이전 판 노트북 34MB)도 여기서 설명합니다.
- 오래 기다린 승객의 위치를 지도에 찍으면 **외곽에 몰려 있습니다.** 11장 연습 11.3(재배치)의 동기입니다.
- 12.9절 보고서 구성 여섯 항목. **6번(한계)을 빼먹지 않게 합니다.**

**연습문제** — 12.1(★) p90 vs 최댓값 · 12.2(★★) 벌점 바꾸면 순위가 뒤집히나 · 12.3(★★★) 공간 형평성 지표

---

## 부록 A 스마트카드 데이터 (목차 제외)

강의에서 다루지 않습니다. 과제와 프로젝트에서 실제 이용 데이터를 쓸 때 참고합니다.

실전 데이터 정리의 함정을 모아 둔 장입니다.

1. **좌표 컬럼 이름이 내용과 어긋납니다** — `X 좌표` 에 위도(35.5)가, `Y 좌표` 에 경도(129.4)가 들어 있습니다. 이름을 믿지 말고 값의 범위로 확인합니다
2. **정류장 표에 중복이 있습니다** — 정산사별로 같은 정류장이 여러 번
3. **일시가 실수형입니다** — `20240923221246.0`. `.0` 을 떼야 파싱됩니다
4. **join 전에 dtype 을 확인합니다** — 한쪽을 숫자로 바꾸면 `int64` 와 `object` 를 못 붙입니다
5. **시군구명이 두 표기로 들어옵니다** — `성남시 분당구` 와 `성남시분당구`. 그냥 집계하면 통행량이 절반으로 갈라집니다

이용자 구분 코드로 교통약자 통행을 따로 볼 수 있습니다. 경로 11.4km/29.9분, 일반 14.2km/35.9분, 어린이 4.3km/14.7분.

---

## 부록 B DTUMOS 서버 운영

교수자용입니다. 학기 준비 체크리스트로도 씁니다.

- **팀당 컨테이너를 따로 띄웁니다.** API 가 프로세스당 시뮬레이션 하나만 돌립니다(`max_workers=1` + 모듈 잠금). 설정으로 못 바꿉니다
- **이미지에 도시 데이터가 없습니다.** `/app/data/cities` 를 마운트해야 합니다
- **인증 없이 외부에 열지 않습니다.** `/api/algorithm/create` 는 사용자 코드 실행 경로이고, `/api/data/region/build` 는 1GB 다운로드를 유발합니다
- **준비된 도시는 넷뿐입니다**(하남·성남·서울·수원). 학기 전에 학생 거주지를 설문으로 받아 미리 준비합니다
- `docker compose up --build` 는 20분 걸립니다. 사전 빌드 이미지를 배포합니다
- 녹화본은 주 1회 `tools/check_fixture_drift.py` 로 실제 엔진과 대조합니다

---

## 코드 자산

### 학생이 직접 짜는 것 (3개)

| 모듈 | 줄 | 검증 기준 |
|---|---|---|
| `teaching/dijkstra.py` | ~120 | NetworkX 와 50쌍 완전 일치. A\*와 다익스트라 결과 동일 |
| `teaching/raptor.py` | ~330 | 손으로 답을 아는 작은 시간표 6케이스 + 실제 피드 불변식 |
| `teaching/simloop.py` | ~200 | 엔진과 평균 대기 1분 이내, 운행 차량 시계열 상관 > 0.8 |

### 읽고 쓰는 것

`teaching/graph.py`(도로망) · `teaching/fare.py`(요금) · `teaching/demand_gen.py`(수요) · `teaching/dispatch.py`(배차·TSP) · `teaching/eta.py`(ETA 특징) · `teaching/metrics.py`(지표)

### 기반

`smartmob/client.py`(엔진 클라이언트) · `smartmob/fixtures.py`(녹화·재생) · `smartmob/data/`(로더·검증) · `smartmob/viz/`(그림)

### 데이터

`data/hanam/` — 도로망 parquet, GTFS parquet, 수요·차량 CSV, O-D, ETA 학습셋 (약 14MB)
`data/fixtures/` — DTUMOS 실행 녹화본 (약 3MB)
`data/smartcard/` — 저장소 공개와 함께 들어냈습니다. 부록 A 는 목차에서 빠져 있습니다

### 검증

테스트 122개. `SMARTMOB_OFFLINE=1 pytest` 로 서버 없이 전부 돕니다.

---

## 12주 배치

| 주 | 날짜 | 교재 | 비고 |
|---|---|---|---|
| 1 | 09-01, 09-02 | 0장, 1장 | 첫 시뮬레이션 |
| 2 | 09-08, 09-09 | 2장 | HW1 출제 |
| 3 | 09-15, 09-16 | 3장 | **직접 구현 1** |
| 4 | 09-22, 09-23 | 4장, 5장 | |
| 5 | 09-29, 09-30 | 6장 | **직접 구현 2** |
| 6 | 10-06, 10-07 | 7장 | HW2, **기말 프로젝트 착수** |
| 7 | 10-13, 10-14 | 8장, 12장 | |
| 8 | 10-20, 10-21 | 휴강 | HW3 (온라인 대체) |
| 9 | 10-27, 10-28 | 중간시험 | 범위 0~8장, 12장 |
| 10 | 11-03, 11-04 | 9장, 10장 | |
| 11 | 11-10, 11-11 | 11장 | **직접 구현 3**, 정규 수업 종료 |
| 12 | 11-17 | 발표 | 기말 프로젝트 |

종강 후 P-실무프로젝트(11-24 ~ 12-11)에서 내가 사는 동네의 버스 노선 개선안을 평가합니다.

---

## 프로젝트

2026-09-09 강의 일정(`smart-transport-logistics/schedule.md`)에 맞춰 두 프로젝트의 자리를 바꿨습니다. 기말이 팀·무당이, P-실무가 내 동네 노선입니다. 스타터 폴더 이름도 `a_campus_shuttle`, `b_neighborhood_transit` 으로 따라 바꿨습니다.

### 기말 팀 프로젝트 — 가천대 교내셔틀 무당이 (`ko/projects/project_a.md`)

6주차 착수 → 12주차 발표. 성적의 40%. 4~5인 팀, 최대 6팀.

**흐름** — 정류장·노선 GeoJSON(6주) → 무당이 GTFS 직접 쓰기 + 승하차 조사(8주, HW3) → 조사표로 수요 프로파일과 현황 재현(9주) → 팀 RAPTOR 로 O-D 100건 질의와 참조 구현 대조(11주) → 팀원별 개선 시나리오 하나씩 사전·사후 비교(12주).

**수업에서 강조할 것**

- 6주차에 시나리오 카드에 적는 "평소 불편했던 것" 하나가 12주차 시나리오가 됩니다. 처음부터 연결해 두면 학기 말에 헤매지 않습니다
- 5장에서 읽기만 하던 GTFS 를 이번에는 **직접 씁니다.** `stop_times.txt` 를 손으로 만들어 보면 형식이 몸에 남습니다. 패턴 수가 운행 수와 같으면 순서 묶기 실패입니다
- 조사표의 observed/estimated 구분이 배점입니다. 구분하지 않으면 감점입니다
- 셔틀 수요는 교시에 묶여 극단적으로 몰립니다. 11.8절의 "배차 알고리즘은 수요가 공급을 압박할 때만 의미가 있다"가 여기서 확인됩니다
- **11장 루프는 정원이 1입니다.** DRT 전환 시나리오를 맡은 학생은 정원을 못 넣어도 한계로 적으면 감점하지 않습니다
- seed 반복이 필요한 이유를 설명해야 합니다. 루프 자체는 결정론적이고 난수는 **수요 생성**에 있습니다

**배포 자료** — `projects/a_campus_shuttle/data/`
`candidate_stops.csv` — 캠퍼스 반경 1.3km 안의 **실제 정류장 27곳**(성남 GTFS 에서 추출, `daily_trips` 포함). 무당이 정류장이 아니라 주변 정류장입니다
`class_schedule.csv` — 교시 시간표 **예시 값**. 학사일정에서 확인해 고치라고 명시되어 있습니다

**스타터** — `projects/a_campus_shuttle/starter/`
`01_class_demand.py`(교시·조사표 기반 수요, 가정을 상수로 노출) · `02_fixed_route.py`(노선 → GTFS 작성) · `03_sweep.py`(스윕 + 파레토)

셋 다 그대로 돌아갑니다. 학생은 상수와 노선을 바꾸는 것부터 시작합니다.

`rubric.md` 는 팀 55점·개인 45점 체크리스트, `report_template.md` 는 팀원별 시나리오 절이 있는 보고서 골격입니다. 강의 저장소 `assignments/project.md` 와 `assignments/project/` 에 같은 내용의 학생 배포본과 양식(조사표, 대조표, GeoJSON 예시, 동료평가)이 있습니다.

### P-실무프로젝트 — 내가 사는 동네의 버스 노선 개선 (`ko/projects/project_b.md`)

종강 후 3주(11-24 ~ 12-11), 3~4인 팀. 주제는 시내버스 노선 조정, 광역버스 노선 평가, 저수요 노선 DRT 전환, 자유주제 중 하나. 11-24 첫 수업일에 확정.

**흐름** — 시군구 경계 고르기 → 전국 GTFS 자르기 → O-D 100쌍 정하기(1주) → 자기 RAPTOR 로 현재 상태 재기, 엔진·지도 앱 대조, 후보안 둘 이상 설계(2주) → 후보안마다 GTFS 고쳐 같은 O-D 로 평가, 파레토 프론트(3주).

**수업에서 강조할 것**

- 노선망 설계 솔버를 만드는 것이 아닙니다. 후보안은 사람이 만들고 시뮬레이션은 평가만 합니다. 파레토에서 고르는 것도 사람입니다
- **데이터에서 이상한 점 찾기가 배점입니다**(20점 중 5점). 들여다봤는지가 여기서 드러납니다
- 서울시는 통짜 폴리곤이라 정류장이 만 개가 넘습니다. 인구 20~50만 시·군을 권합니다
- 대중교통만 다루면 GTFS 만 있으면 되므로 어느 시군구든 됩니다. 도로망이 필요한 것은 DRT 전환 주제뿐이고 서버에 준비된 4개 도시 안에서 고릅니다
- 후보안이 하나뿐이면 감점입니다. 파레토를 그리려면 둘 이상이어야 합니다

**스타터** — `projects/b_neighborhood_transit/starter/`
`01_clip_gtfs.py`(대상지 자르기, 완성) · `02_baseline.py`(O-D 고르는 부분이 빈칸) · `03_scenario.py`(GTFS 고치는 부분이 빈칸)

`rubric.md` 는 학생이 제출 전 체크하는 항목별 배점, `report_template.md` 는 보고서 골격입니다.

---

## 실습 (`labs/`)

교재의 장마다 노트북이 하나씩 있습니다. 파일 이름이 곧 장 번호입니다.
수업 중에는 교재를 화면에 띄우고 학생은 같은 번호의 노트북을 엽니다.

| 노트북 | 교재 | 수업 중에 하는 일 |
|---|---|---|
| `ch00_setup.ipynb` | 0장 | 설치 확인, 첫 DTUMOS 시뮬레이션 |
| `ch01_why.ipynb` | 1장 | 대기시간 분포, 평균의 함정 |
| `ch02_road_network.ipynb` | 2장 | 도로망 표, `modes` 필터 전후 |
| `ch03_dijkstra.ipynb` | 3장 | **직접 구현 1** — 작은 그래프 → 하남 → NetworkX 대조 |
| `ch04_speeds_engine.ipynb` | 4장 | 시간대별 소요시간 곡선, 경로가 갈리는 것 |
| `ch05_gtfs.ipynb` | 5장 | GTFS 표 다섯 개, 경계로 자르기 |
| `ch06_raptor.ipynb` | 6장 | **직접 구현 2** — 작은 시간표 → 하남 GTFS → 불변식 |
| `ch07_raptor_fare.ipynb` | 7장 | 지표 분포 여섯 개, 환승 허용 횟수의 포화 |
| `ch08_demand.ipynb` | 8장 | 수요 3단계, 만든 수요를 루프에 바로 투입 |
| `ch09_eta.ipynb` | 9장 | 기준선 → 선형회귀 → LightGBM, 비용행렬 투입 |
| `ch10_dispatch.ipynb` | 10장 | 완전탐색으로 헝가리안 검증 |
| `ch11_simloop.ipynb` | 11장 | **직접 구현 3** — 엔진 녹화본과 나란히 비교 |
| `ch12_metrics.ipynb` | 12장 | 파레토 곡선, 보고서용 표, 웹 뷰어로 내보내기 |
| `ch12_viewer/` | 12장 | 통행 애니메이션 뷰어를 직접 만듭니다 (자바스크립트) |

**수업 진행 방식** — 각 장 끝의 「이 장의 실습」이 어느 노트북인지 알려 줍니다.
노트북의 빈칸은 `None` 으로 비어 있고, 채우면 그 자리에서 맞는지 알려 줍니다(`labs/lab.py` 의 `todo`, `expect`).
빈칸을 채우지 않아도 노트북은 끝까지 돌아갑니다. 처음 열었을 때 오류가 나면 학생이 시작을 못 하기 때문입니다.

**채점받는 셋** — 3·6·11장은 노트북이 아니라 옆의 `.py` 파일을 채웁니다.
채점기와 `tests/` 가 그 파일을 직접 불러다 쓰기 때문입니다.
노트북에 `%autoreload` 를 켜 두어서, 편집기에서 고치고 저장하면 셀만 다시 실행하면 됩니다.

| 파일 | 교재 | 주차 | 채점 기준 |
|---|---|---|---|
| `labs/ch03_dijkstra.py` | 3장 | 3주차 | NetworkX 와 30쌍 완전 일치 |
| `labs/ch06_raptor.py` | 6장 | 5주차 | 손으로 답을 아는 시간표 5케이스 + 실제 피드 불변식 2개 |
| `labs/ch11_simloop.py` | 11장 | 11주차 | 엔진과 평균 대기 1.5분 이내, 출력 형식 일치 |

```bash
python labs/check.py ch03      # 하나씩
python labs/check.py all       # 전부
```

`check.py` 가 보는 것은 `tests/` 가 보는 것과 같습니다. **채점 기준을 미리 공개합니다.**
무엇과 대조해야 맞는 것인지 아는 것이 이 과목에서 배우는 것의 절반이기 때문입니다.

**느린 구현으로 빠른 구현을 검증하는 자리** — 이 교재가 반복해서 쓰는 구조입니다.
3장은 NetworkX 로, 6장은 손으로 답을 아는 시간표로, 10장은 모든 경우를 세어 보는 완전탐색으로,
11장은 DTUMOS 엔진 녹화본으로 자기 코드를 채점합니다.
10장의 완전탐색은 `scipy` 의 헝가리안 구현을 믿지 않고 직접 확인해 보는 자리입니다.

**12장 웹 뷰어** — 이 책에서 파이썬이 아닌 코드를 학생이 채우는 유일한 자리입니다.

DTUMOS 프론트엔드는 React 와 deck.gl 로 통행 컴포넌트 하나가 1,800줄이 넘습니다.
그중 애니메이션의 뼈대는 셋뿐입니다. `TripsLayer` 에 좌표열과 시각열을 넣고,
매 프레임 `currentTime` 을 늘리고, 다시 그립니다.
`labs/ch12_viewer/` 는 그 셋만 남긴 프로젝트입니다.
DTUMOS 와 같이 `npm run dev` 로 실행하되, 의존성은 deck.gl 셋과 Vite 하나뿐입니다.
DTUMOS 프론트엔드가 30개가 넘는 것과 비교됩니다.
학생이 고치는 파일은 `src/main.js` 하나이고, 나머지는 배선입니다.

**수업에서 강조할 것**

- 내보낸 파일 이름과 키는 DTUMOS 가 내는 것 그대로입니다. 서버에서 받은 결과 디렉터리를
  그대로 넣어도 돌아갑니다. 형식을 맞춰 두면 남의 도구를 그대로 쓸 수 있다는 예입니다
- 11장에서 직접 짠 루프의 결과도 같은 함수로 내보냅니다. 그쪽은 통행이 직선 하나로 나옵니다.
  그 직선이 곧 그 모형의 근사 수준입니다. 엔진 결과와 나란히 보면 차이가 눈에 보입니다
- 파이썬으로 하지 않는 이유를 짚습니다. `TripsLayer` 는 시각을 그래픽 카드로 넘겨서
  통행 수천 개의 꼬리를 매 프레임 다시 계산하지 않습니다. 도구는 문제에 맞춰 고릅니다
- 발표에서 쓸 결과물이 여기서 나옵니다. 프로젝트 제출물이 지금까지 전부 정적이었습니다.
  `npm run build` 결과를 깃허브 페이지에 올리면 공개 주소가 생깁니다
- 저장하면 브라우저가 바로 다시 그립니다. 빈칸을 하나씩 채워 가며 확인하기 좋습니다
- **Node 20.19 이상이 필요합니다.** 12주차 전에 설치를 안내하세요.
  `npm install` 이 취약점 경고를 내는데, deck.gl 이 3D 타일을 읽으려고 딸려 오는
  패키지 때문입니다. 이 실습은 그 경로를 부르지 않습니다.
  `npm audit fix --force` 는 deck.gl 을 낮춰 빌드를 깨뜨리므로 실행하지 않습니다

`tools/check_viewer.mjs` 가 브라우저 없이 채점합니다. `src/main.js` 의 다섯 함수는
화면과 무관한 순수 함수라서 그대로 불러다 확인할 수 있습니다.
빈칸판이 전부 실패하는지, 정답 구현이 전부 통과하는지 두 방향을 다 봅니다.
`labs/check.py` 가 파이썬 실습을 채점하는 것과 같은 자리입니다.

**어긋나지 않게 하는 장치** — 두 가지가 자동으로 돕니다.

- `tests/test_labs.py` — 장마다 「이 장의 실습」 절이 있는지, 그 장 번호의 노트북을 가리키는지,
  그 파일이 실제로 있는지, 노트북에 실행 결과가 남아 있지 않은지 확인합니다
- `tools/run_labs.py` — 노트북 13개를 빈칸이 빈 상태 그대로 전부 실행합니다
- `tools/check_viewer.mjs` — 12장 웹 뷰어의 빈칸을 채점합니다
- `npm run build` — 뷰어 전체가 컴파일되는지 확인합니다
- `.github/workflows/ci.yml` 이 셋을 풀 리퀘스트마다 실행합니다

`tests/test_exercises.py` 는 빈칸판 자체를 봅니다. 빈칸판은 전부 실패해야 하고(실수로 정답이 남으면 안 됩니다),
정답 구현은 전부 통과해야 합니다(기준이 달성 가능해야 합니다).
일부러 5% 틀린 구현을 넣어 채점기가 잡아내는지도 봅니다.

---

## 남은 일

- [ ] 대용량 자산 외부 배포 후 `data/MANIFEST.toml` 의 url·sha256 채우기
- [ ] `references.bib` 교체 + 본문 `{cite:p}` 삽입
- [ ] 사전 빌드 Docker 이미지 배포, 공용 서버 구축
- [ ] 중간시험 문제 세트 (범위가 바뀌어 이전 문제 재활용 불가)
- [ ] `class_schedule.csv` 를 실제 가천대 교시 시간표로 교체
- [ ] 학생 거주지 설문 → 상위 10개 시군구 사전 빌드
