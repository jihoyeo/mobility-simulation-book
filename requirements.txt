# 책 빌드 + 본문 실습용. Python 3.11 기준.
# 무거운 의존성(lightgbm, scikit-learn, osmnx)은 requirements-heavy.txt 로 분리한다.
#
#   pip install -r requirements.txt
#
# jupyter-book 2.x 는 MyST-MD 기반 완전 재작성이라 이 책과 호환되지 않는다. 1.x 로 고정한다.
# sphinx 는 jupyter-book 이 해석하도록 두고 직접 고정하지 않는다.

jupyter-book==1.0.4.post1
myst-nb==1.1.2
jupytext==1.16.4

pandas==2.3.3
numpy==2.1.3
pyarrow==18.1.0
scipy==1.14.1

geopandas==1.0.1
shapely==2.0.6
networkx==3.4.2

matplotlib==3.9.2
seaborn==0.13.2
folium==0.19.3
pydeck==0.9.1

scikit-learn==1.5.2
lightgbm==4.5.0

requests==2.32.3
tqdm==4.67.1
python-dotenv==1.0.1

-e .
